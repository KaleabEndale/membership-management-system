<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
          body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #e3f2fd, #bbdefb);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            flex-direction: column;
        }

        h1 {
            color: #333;
            font-size: 2.5rem;
            margin-bottom: 20px;
            text-align: center;
        }

        form {
            background-color: #fff;
            padding: 20px;
            margin: 10px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 350px;
            text-align: center;
        }

        label {
            font-weight: bold;
            margin: 5px;
            display: inline-block;
            color: #555;
        }

        select, button {
            padding: 10px;
            margin: 5px;
            width: 80%;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
            background-color: #42a5f5;
            color: #fff;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover, select:hover {
            background-color: #1e88e5;
        }

        button.logout {
            background-color: #f44336;
        }

        button.logout:hover {
            background-color: #d32f2f;
        }
    </style>
</head>
<body>
    <h1><%= locals.cp %></h1>
    <form id="f" action="/member/changepass" method="post">

        <input type="hidden" name="lang" value="<%= locals.lang %>">
        <label><%= locals.op %></label>
        <input type="password" minlength="8" required name="oldpassword" value="<%= locals.oldpassword %>">

        <label><%= locals.np %></label>
        <input type="password" minlength="8" required name="newpassword" value="<%= locals.newpassword %>">
        <button type="submit"><%= locals.cpb %></button><br>
        <label><%= locals.mu %></label>
    </form>

</body>
</html>