<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Member</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
        }

        h1 {
            color: #333;
            text-align: center;
        }

        #f {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            width: 350px;
            margin: 0 auto;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
        }

        input[type="text"],
        input[type="tel"],
        input[type="email"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
        }

        button {
            width: 100%;
            padding: 10px;
            background-color: #42a5f5;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }

        button:hover {
            background-color: #1e88e5;
        }

        .error-message {
            color: red;
            font-size: 14px;
            text-align: center;
            margin-top: 10px;
        }
    </style>
</head>
<body>
       <form  action="/aved/back" method="post">
     <button type="submit">BACK</button>
</form>
    <h1>EDIT MEMBER</h1>

    <form id="f" action="/aved/editmember" method="post">
        <label>Username: <%= locals.username %></label><br>
        <input type="hidden" name="username" value="<%= locals.username %>">
        
        <label>Contact</label><br>
        <input type="tel" required pattern="^\+\d{7,15}$" name="contact" value="<%= locals.contact %>"><br>
        
        <label>Address</label><br>
        <input type="text" required name="address" value="<%= locals.address %>"><br>
        
        <label>Email</label><br>
        <input type="email" required name="email" value="<%= locals.email %>"><br>

        <button type="submit">Edit</button><br>
    </form>

    <div class="error-message">
        <label><%= locals.mu %></label>
    </div>
</body>
</html>
