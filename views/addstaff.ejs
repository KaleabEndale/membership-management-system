<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Staff</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            background-color: #f9f9f9;
        }

        h1 {
            text-align: center;
            margin-top: 20px;
        }

        #f {
            background-color: #fff;
            border-radius: 8px;
            border: 1px solid #ddd;
            padding: 20px;
            margin-top: 30px;
            width: 400px;
            margin-left: auto;
            margin-right: auto;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        label {
            font-weight: bold;
            margin-top: 10px;
            display: block;
        }

        input {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border-radius: 5px;
            border: 1px solid #ddd;
        }

        button {
            background-color: #4CAF50;
            color: white;
            padding: 12px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
        }

        button:hover {
            background-color: #45a049;
        }

        .error-label {
            color: red;
            font-size: 14px;
            display: block;
            margin-top: 10px;
        }

        .success-label {
            color: green;
            font-size: 14px;
            display: block;
            margin-top: 10px;
        }
    </style>
</head>
<body>
        <form  action="/aved/back" method="post">
     <button type="submit">BACK</button>
</form>
    <h1>Add Staff</h1>

    <form id="f" action="/aved/addstaff" method="post">
        <label for="username">Username</label>
        <input type="text" id="username" required name="username" value="<%= locals.username %>"><br>

        <label for="contact">Contact</label>
        <input type="tel" id="contact" required pattern="^\+\d{7,15}$" name="contact" value="<%= locals.contact %>"><br>

        <label for="address">Address</label>
        <input type="text" id="address" required name="address" value="<%= locals.address %>"><br>

        <label for="email">Email</label>
        <input type="email" id="email" required name="email" value="<%= locals.email %>"><br>

        <label for="password">Password</label>
        <input type="password" id="password" minlength="8" required name="password" value="<%= locals.password %>"><br>

        <button type="submit">Add Staff</button><br>
    </form>

    <label class="error-label"><%= locals.er %></label>
    <label class="success-label"><%= locals.ma %></label>
</body>
</html>
